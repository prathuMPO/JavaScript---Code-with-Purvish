// Immediately invoked functions expressions(iife)

// (function chaiTea(){

// named IIFE hain
//     console.log(`DB CONNECTED`);
// })()

// (function defenation)(func execution) 

( (name) => {
    console.log(`Db connected iwo ${name}`);
})('HCaiuop');